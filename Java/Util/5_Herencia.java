public class Herencia {
    public static void main(String[] args) {
        /*
        
        HERENCIA:

        Para poder crear una herencia se debe a través de la CLASE extender atributos del padre. La palabra clave xtends le dice a nuestor compilador la relación y ahora la clase Human ha heredado todos los miembros de la súper clase (excepto que no tendrá acceso a los atributos privados a menos que estén expuestos a través de getters y setters).

        ¿Qué pasa si no quiero que cada método sea el mismo? Entonces podría sobre escribir el método de la súper clase utilizando la palabra override.


        SOBRE ESCRIBIR MÉTODOS:

        Se puede cambiar o extender el método de una súper clase solo escribiendo un método con la misma firma del método y que devuelva el mismo valor (argumentos). Luego este método será llamado de la subclase cada vez que sea invocado.

        */
    }
}