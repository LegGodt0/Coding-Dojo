public class Variables {
    public static void main(String[] args) {
        
        /* 

        Tipos de variable: 

        - Variables globales - atributos del objeto o campos.
        - Variables locales - variables en un bloque de código / método que no son variables asociadas de la clase (estos son los primeros tipos de variables que ha usado hasta ahora).
        - Variables parámetro - estas son declaradas en su método.

        Getters y setters:

        Son la forma de acceder a las variables globales, ya que son declaradas en private (para prevenir el acceso directamente al campo). Los getters recuperan el valor de un atributo y lo setters establecen el valor de los atributos

        NOTA: "this" especifíca que la variable que se está usando es la GLOBAL, entonces si hay una variable local con el mismo nombre no la tomará en cuenta.
        */
    }
}
