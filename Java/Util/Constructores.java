public class Constructores {
    public static void main(String[] args) {
        
        /*
        Un método constructor es una función especial que se llama cuando una instancia del objeto es creada. Las diferencias entre un constructor y métodos comúnes son:

        - No tiene return
        - El nombre del constructor DEBE SER el mismo nombre de la clase a la que pertenece
        - Un constructor es llamado automáticamente cuando una instancia de un objeto es creada

        Estos métodos se utilizar para ejecutar un bloque de código cuando se crea un objeto. Básicamente le hace la pega a los getters y setters dejando todo en un solo método.

        Sobrecarga de métodos:

        - Esto se refiere a definir una nueva firma para el mismo nombre de método. Es decir, podemos darle la cantidad de variables que queramos.

        NOTA: Cada vez que se agrega una variable dentro de un projecto se debe atender:
        - Agregar variable global al projecto (private)
        - Agregar al constructor la variable
        - Agregar en el cuerpo del constructor la asignación de la variable
        - Agregar sus respectivos getters and setters
        - Luego en el método de impresión tú verás donde lo pones dentro del return

        Además los valores de las variables globales se dan dentro del TEST
        */
    }
}
